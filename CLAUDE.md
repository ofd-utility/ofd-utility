# 工程结构
- ofd-core核心API工程
- ofd-cli命令行工程


# 工程规范
- 日志内容包含时间、方法、参数等内容
- 模块化设计
- 单文件不超过1000行代码
- 复用已有函数，若参数不一样，优先考虑重构老函数。减少代码量。

# 测试
- 分支覆盖率必须大于等于90%
- 覆盖率工具为 cargo-llvm-cov；运行 `scripts/coverage.sh`（加 `--html` 出网页报告）。
- 环境无 rustup，脚本已指向系统 LLVM 工具；stable rustc 不产出分支计数，以 region
- 覆盖率作为分支覆盖的工程近似，门槛同取 90%。
- 测试文件单独文件，禁止与源代码在一个文件


